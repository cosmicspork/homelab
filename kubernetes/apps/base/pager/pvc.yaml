apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pager-data
  namespace: pager
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: do-block-storage-retain
  resources:
    requests:
      storage: 1Gi
